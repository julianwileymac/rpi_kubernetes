# =============================================================================
# Kustomization - BentoML
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: bentoml

namespace: ml-platform

resources:
  - secret.yaml
  # Note: BentoML/Yatai is deployed via Helm
  # See README.md for installation instructions
