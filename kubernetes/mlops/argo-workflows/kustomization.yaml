# =============================================================================
# Kustomization - Argo Workflows
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: argo-workflows

namespace: mlops

resources:
  - secret.yaml
  # Note: Argo Workflows is deployed via Helm
  # See README.md for installation instructions
