# =============================================================================
# MinIO Credentials for Argo Workflows
# =============================================================================
# This secret is used by Argo Workflows to store artifacts in MinIO.
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: argo-workflows-minio
  namespace: mlops
type: Opaque
stringData:
  accesskey: minioadmin
  secretkey: minioadmin123
