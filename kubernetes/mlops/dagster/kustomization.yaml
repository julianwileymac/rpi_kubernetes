apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: dagster

namespace: mlops

resources:
  - secret.yaml
  - user-code-bootstrap-configmap.yaml
  # Note: Dagster runtime components are deployed via Helm.
  # See README.md for installation instructions.
