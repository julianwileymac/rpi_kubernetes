apiVersion: flink.apache.org/v1beta1
kind: FlinkSessionJob
metadata:
  name: indicators-cycle
  namespace: flink
  labels:
    app: flink-trading-session
    job: indicators-cycle
    pipeline-stage: compute
    category: cycle
spec:
  deploymentName: flink-trading-session
  job:
    jarURI: s3://flink-jobs/java/indicators-cycle.jar
    entryClass: io.rpi.flink.indicators.cycle.CycleJob
    args:
      - "--kafka.bootstrap.servers"
      - "trading-kafka-kafka-bootstrap.data-services.svc.cluster.local:9092"
      - "--kafka.source.topic"
      - "market.bar.v1"
      - "--kafka.sink.topic"
      - "features.indicators.v1"
      - "--kafka.group.id"
      - "flink-java-cycle"
      - "--schema.registry.url"
      - "http://apicurio-registry.data-services.svc.cluster.local:8080/apis/registry/v2"
      - "--category.label"
      - "cycle"
      - "--buffer.size"
      - "120"
    parallelism: 1
    upgradeMode: savepoint
    state: suspended
