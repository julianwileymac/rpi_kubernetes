apiVersion: v1
kind: Service
metadata:
  name: polaris
  namespace: data-services
spec:
  type: ClusterIP
  selector:
    app: polaris
  ports:
    - name: api
      port: 8181
      targetPort: api
    - name: management
      port: 8182
      targetPort: management
