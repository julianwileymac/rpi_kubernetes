# =============================================================================
# Kustomization - RAGFlow
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: ragflow

namespace: data-services

resources:
  - secret.yaml
  - configmap.yaml
  - pvc.yaml
  - redis-deployment.yaml
  - deployment.yaml
  - service.yaml
