apiVersion: v1
kind: Service
metadata:
  name: vector
  namespace: observability
  labels:
    app: vector
spec:
  type: ClusterIP
  selector:
    app: vector
  ports:
    - name: api
      port: 8686
      targetPort: 8686
      protocol: TCP
