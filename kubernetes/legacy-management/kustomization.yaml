apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Rollback-only legacy management surface.
#
# Default cluster bootstrap no longer deploys this stack. New AQP workload
# control belongs in agentic_quant_platform/aqp_control_plane and is reached
# through the rpi_k8s_sdk.AqpControlPlaneClient or `aqp cp ...`.
resources:
  - ../base-services/management/

